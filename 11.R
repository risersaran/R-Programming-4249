# Get unique elements of a string
unique_chars <- unique(strsplit("hello world", "")[[1]])
print(unique_chars)

# Get unique numbers of a vector
unique_numbers <- unique(c(1, 2, 3, 4, 2, 3, 5, 6, 1, 7))
print(unique_numbers)
