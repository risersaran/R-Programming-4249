# Set seed for reproducibility (optional)
set.seed(123)

# Create vector with 10 random integer values between -50 and +50
random_vector <- sample(seq(-50, 50), 10, replace = TRUE)

# Print the random vector
print(random_vector)
